spring:
  datasource:
    url: jdbc:h2:mem:sdw2023
    username: sdw2023
    password:
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
    open-in-view: true
    hibernate:
      ddl-auto: create #validate | update | create | create-drop
    properties:
      hibernate:
        format-sql:true

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-orthers: false
